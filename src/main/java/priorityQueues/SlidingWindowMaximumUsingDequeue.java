package priorityQueues;

import java.util.*;

//TODO(LATER)
public class SlidingWindowMaximumUsingDequeue {

    public static void main(String[] args) {
        int[] input = {1, 2, 3, 1, 4, 5, 2, 3, 6};
        int window = 3;

        List<Integer> result = new SlidingWindowMaximumUsingDequeue().getSlidingWindowMaximumUsingDequeue(input, window);

        result.forEach(System.out::println);
    }

    private List<Integer> getSlidingWindowMaximumUsingDequeue(int[] input, int window) {
//TODO: complete this method
        return null;
    }
}
